//write function average which calculates the average of a given list
//create an array, function adds values of array and divides by the number of elements(length of the array)

var myArray = [];


function findAverage (myArray){
var total=0;
for(var i = 0; i < myArray.length; i++){

total += myArray[i];

  }
}
var avg = total / myArray.length;
